# Database Credentials
DB_ROOT_PASSWORD=change_me_root_password
DB_NAME=synergyhub
DB_USER=synergy
DB_PASSWORD=change_me_user_password

# Backend Security
# Generate a secure random string for JWT (min 64 chars)
JWT_SECRET=change_me_jwt_secret
# Generate a secure random string for DB Encryption (32 chars)
DB_ENCRYPTION_KEY=change_me_db_encryption_key

# MinIO Connection (Existing)
MINIO_ENDPOINT=http://minio.example.com:9000
MINIO_ACCESS_KEY=your_minio_access_key
MINIO_SECRET_KEY=your_minio_secret_key

# CORS (Frontend Domain)
CORS_ALLOWED_ORIGINS=https://your-vietnam-domain.vn

# LiveKit (Video)
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret
LIVEKIT_URL=http://your-vps-ip:7880
