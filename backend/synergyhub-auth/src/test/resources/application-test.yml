spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  
  mail:
    host: localhost
    port: 1025
    test-connection: false

app:
  jwt:
    secret: testSecretKeyThatIsAtLeast256BitsLongForHS256Algorithm
    expiration: 86400000
  
  frontend:
    url: http://localhost:3000
  
  mail:
    enabled: false

logging:
  level:
    com.synergyhub: ERROR
    org.springframework: ERROR