spring:
  datasource:
    url: jdbc:mysql://localhost:3306/synergy_hub?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    com.synergyhub: DEBUG

app:
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://localhost:8080
      - http://localhost:5173  # Vite default
